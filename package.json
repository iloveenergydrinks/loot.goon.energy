{
  "name": "looting",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Per-item node-based looting/salvage simulation core with queue, hazard, and detection.",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "start": "npx serve dist -s -l $PORT",
    "demo": "tsx src/cli.ts",
    "test": "vitest run",
    "web:dev": "vite",
    "web:build": "vite build",
    "web:preview": "vite preview"
  },
  "dependencies": {
    "vite": "^5.3.5",
    "serve": "^14.2.0"
  },
  "devDependencies": {
    "tsx": "^4.16.2",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}


